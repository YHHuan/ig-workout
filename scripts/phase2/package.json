{
  "name": "ig-workout-phase2-scripts",
  "private": true,
  "type": "module",
  "description": "One-shot migration scripts for Phase 2 (R2 + Supabase). Not shipped; not imported by site/.",
  "scripts": {
    "migrate": "node --env-file=../../.env migrate.mjs",
    "migrate:dry": "node --env-file=../../.env migrate.mjs --dry-run"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@supabase/supabase-js": "^2.45.0",
    "pg": "^8.20.0"
  }
}
